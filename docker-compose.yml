services:
  node-app:
    build: .
    platform: linux/amd64
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
    networks:
      - app-network

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./static:/usr/share/nginx/html
    depends_on:
      - node-app
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
